import React from "react";
import { WhiteboardWithEditor } from "../components/whiteboard";

type Props = {};

const Whiteboard = (props: Props) => {
  return (
    <div>
      <WhiteboardWithEditor />
    </div>
  );
};

export default Whiteboard;
